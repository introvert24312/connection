# 修复功能测试指南

## 测试环境准备
1. 启动应用
2. 确保有一些带地点标签的节点数据
3. 准备测试两种窗口模式：主窗口和独立窗口

## 测试 1: Command+P 位置选择器功能

### 主窗口测试（应该一直工作）
1. 在主窗口按 `Command+I` 打开快速添加
2. 在输入框中按 `Command+P`
3. **期望结果**: 
   - 地图窗口应该打开
   - 控制台显示 "✅ 主窗口: 处理指定给主窗口的openMapWindow通知"
   - 地图窗口标题显示 "选择位置"

### 独立窗口测试（修复后应该工作）
1. 按 `Command+B` 打开独立窗口
2. 在独立窗口按 `Command+I` 打开快速添加
3. 在输入框中按 `Command+P`
4. **期望结果**:
   - 地图窗口应该打开
   - 控制台显示 "✅ 独立窗口: 处理指定给当前独立窗口的openMapWindow通知"
   - 地图窗口应该正确链接到独立窗口（不是主窗口）

### 关键调试信息
查看控制台输出中的以下信息：
```
📍 QuickAddSheetView: 确定源窗口ID: [窗口ID前8位]
🎯 [窗口类型]: 收到带源窗口ID的openMapWindow通知 - [窗口ID前8位]
✅ MapWindow: 立即设置窗口映射 - 源窗口: [窗口ID前8位]
```

## 测试 2: 地图点击精确定位功能

### 主窗口测试
1. 在主窗口打开地图 (`Command+M`)
2. 点击地图上的任意标注点
3. **期望结果**:
   - 主窗口应该切换到对应的层
   - 左侧标签面板应该展开"地点"标签类型
   - 应该选中具体的地点标签（如"天马广场"）
   - 应该在节点列表中选中对应的节点
   - 详情面板应该切换到地图标签页

### 独立窗口测试
1. 在独立窗口打开地图 (`Command+M`)
2. 点击地图上的任意标注点
3. **期望结果**:
   - 独立窗口应该切换到对应的层
   - 独立窗口的标签面板应该展开"地点"标签类型
   - 应该选中具体的地点标签值
   - 应该在独立窗口的节点列表中选中对应的节点

### 关键调试信息
查看控制台输出中的以下信息：
```
🗺️ [窗口类型]: 处理地图标注点击 - 节点: [节点名], 层: [层名]
🗺️ Store.expandLocationTagAndSelect: 开始完整标签导航流程 '[节点名]'
✅ [窗口类型]: 已切换到层 '[层名]' 并展开地点标签
```

## 错误排查

### 如果 Command+P 仍然失效
1. 检查控制台是否显示源窗口ID确定信息
2. 检查是否有 "🚫 忽略发给其他窗口的通知" 信息
3. 确认 WindowFocusManager 正确识别了活跃窗口

### 如果地图点击定位失效
1. 检查是否有 "⚠️ 未在当前store中找到目标节点" 信息
2. 确认节点ID和层ID是否正确传递
3. 检查独立窗口的store实例是否包含相同的数据

## 成功标准
- ✅ 主窗口和独立窗口的 Command+P 都能正常工作
- ✅ 主窗口和独立窗口的地图点击都能精确定位到节点
- ✅ 控制台没有错误信息
- ✅ 窗口通知路由正确，没有串扰现象