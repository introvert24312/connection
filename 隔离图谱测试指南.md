# 🛡️ 隔离图谱测试指南

## 🎯 隔离方法

我已经创建了一个**完全隔离**的图谱组件，它：

### 🚫 不依赖任何外部服务
- ❌ 不使用 `WindowFocusManager`
- ❌ 不使用 `WebViewUpdateManager`
- ❌ 不使用 `GraphManager`
- ❌ 不监听 `NSApplication` 状态变化
- ❌ 不接受任何外部通知

### 🛡️ 完全独立运行
- ✅ 使用独立的进程池 `WKProcessPool()`
- ✅ 使用独立的数据存储 `WKWebsiteDataStore.nonPersistent()`
- ✅ 使用简单的SVG渲染，不依赖外部JavaScript库
- ✅ 在独立的队列中计算图谱数据

### 🎨 可视化标识
- 🛡️ 橙色边框调试信息显示 "隔离图谱"
- 🛡️ WebView内部有橙色 "隔离模式" 标签
- 🛡️ 日志前缀为 "🛡️ IsolatedGraphView"

## 🧪 测试步骤

### 1. 启动测试
1. 编译并运行应用
2. 点击任意节点
3. 切换到 "图谱" tab

**期望结果**:
- 看到橙色边框的 "🛡️ 隔离图谱" 调试信息
- 看到简单的SVG圆形图谱
- 日志显示: `🛡️ IsolatedGraphView: 开始隔离加载`

### 2. 焦点变化测试 (关键测试)
1. 确保图谱正常显示
2. **切换到其他应用** (浏览器、访达等)
3. **切换回WordTagger**

**期望结果**:
- ✅ 图谱应该立即可见，没有任何闪烁
- ✅ 不应该看到任何 "DISAPPEARED" 日志

### 3. 多次切换测试
1. 反复切换应用焦点 5-10 次
2. 观察图谱是否始终稳定显示

**期望结果**:
- ✅ 图谱始终稳定显示
- ✅ 没有重新加载或闪烁

## 🔍 调试信息解读

### 正常启动日志:
```
🛡️ IsolatedGraphView: 开始隔离加载 - [节点名]
🛡️ IsolatedGraphView: 隔离计算完成 - X节点, Y边
🛡️ IsolatedWebView: 创建隔离WebView - ID: [8位ID]
🛡️ DetailPanel: ISOLATED GraphView APPEARED for node: [节点名]
🛡️ IsolatedCoordinator[ID]: 隔离WebView加载完成
```

### 🚨 问题日志 (如果出现说明隔离失败):
```
❌ DetailPanel: ISOLATED GraphView DISAPPEARED for node: [节点名]
```

## 📋 测试报告

请测试后告诉我：

1. **是否看到橙色的隔离标识**?
2. **焦点切换时图谱是否稳定**?
3. **Console中的日志内容**?
4. **如果仍有问题，具体是什么现象**?

这个隔离版本应该能彻底解决问题，因为它完全不受任何外部干扰！