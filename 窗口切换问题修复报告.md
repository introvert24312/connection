# 窗口切换问题修复报告

## 🔍 问题诊断

通过详细的调试日志分析，发现了问题的根本原因：

### 原始问题
- **现象**: 用户点击窗口A，但Command+点击层图谱时切换到了窗口C
- **用户描述**: "切窗口这么简单的事情，为什么很复杂？？为什么这么难分清？"
- **调试显示**: 日志显示正确，但实际行为错误

### 根本原因分析
从实际调试日志中发现：

1. **窗口映射混淆**: 
   - 多个窗口使用相同的emoji标题: `(* ¯ ︶ ¯ *)ノ ❤ ヽ( ^ ^ )`
   - 不同的NSWindow对象 (`ObjectIdentifier(0x000000013fabb000)` vs `ObjectIdentifier(0x000000013db83a00)`)
   - 都被错误地映射到同一个UUID: `C8B9E23F`

2. **用户点击检测正确**:
   ```
   🎯 LayerGraphWindow: 用户真实点击了窗口，锁定目标 - (22D6F422)
   🎯 LayerGraphWindow: 用户真实点击了窗口，锁定目标 - (7C336797)
   ```

3. **NSWindow映射系统失效**:
   - `handleWindowSwitchWithWindow()` 总是通过NSWindow映射找到 `C8B9E23F`
   - 忽略了用户实际点击的窗口ID

## 🔧 修复方案

### 核心修复 (LayerGraphWindowView.swift:358-376)

修改 `handleWindowSwitchWithWindow()` 方法，**优先使用WindowFocusManager的活跃窗口**而不是NSWindow映射：

```swift
// 🔧 修复关键问题：优先使用 WindowFocusManager 的活跃窗口而非 NSWindow 映射
// 因为 NSWindow 映射可能不准确（多个窗口相同标题时）
if let activeWindowId = windowManager.getActiveWindowId(),
   let activeWindowInfo = windowManager.getWindowInfo(for: activeWindowId),
   activeWindowInfo.type == .standard,
   activeWindowId.uuidString != windowId.uuidString {
    
    let previousTarget = lastActiveTargetWindowId
    lastActiveTargetWindowId = activeWindowId.uuidString
    
    print("\n🎯 === 目标窗口更新（优先使用活跃窗口）===")
    print("   ✅ 使用 WindowFocusManager 的活跃窗口而非 NSWindow 映射")
    return
}
```

### 修复原理

1. **信任WindowFocusManager**: WindowFocusManager已经正确跟踪了用户点击的窗口
2. **避免NSWindow映射冲突**: 当多个窗口标题相同时，NSWindow映射不可靠
3. **保持回退机制**: 如果活跃窗口方案失败，仍然使用NSWindow映射作为备用

## ✅ 预期修复效果

修复后应该看到：
- 调试日志显示 `✅ 使用 WindowFocusManager 的活跃窗口而非 NSWindow 映射`
- Command+点击层图谱后，**正确的窗口**被激活（与用户最后点击的窗口一致）
- 不再出现"点击窗口A但切换到窗口C"的问题

## 🧪 测试步骤

1. **启动应用，打开多个主窗口（相同emoji标题）**
2. **打开层图谱窗口**
3. **点击窗口A** - 观察日志显示正确的窗口ID被记录
4. **Command+点击层图谱节点** - 验证窗口A被激活，而非其他窗口

## 📝 技术要点

- **文件**: `WordTagger/LayerGraphWindowView.swift`
- **修改行数**: 358-376
- **核心思想**: 简化窗口选择逻辑，信任现有的WindowFocusManager系统
- **后向兼容**: 保留了NSWindow映射作为回退机制

这个修复贯彻了用户的反馈："切窗口这么简单的事情，为什么很复杂？" - 通过简化逻辑并使用已经正确工作的WindowFocusManager来解决问题。