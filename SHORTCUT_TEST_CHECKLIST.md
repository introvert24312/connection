# WordTagger 快捷键测试清单

## 测试环境准备
1. ✅ 启动WordTagger应用
2. ✅ 确保主窗口正常显示
3. ✅ 使用Command+B创建2-3个独立窗口
4. ✅ 在不同窗口间切换以测试焦点管理

## 快捷键测试清单

### 全局命令（应该在任何活跃窗口中工作）

#### Command+K（命令面板）
- [ ] 在主窗口中按Command+K → 只在当前窗口显示命令面板
- [ ] 切换到独立窗口，按Command+K → 只在当前窗口显示命令面板
- [ ] 连续快速按Command+K → 不应该重复打开

#### Command+B（新建窗口）
- [ ] 在主窗口中按Command+B → 创建一个新的独立窗口
- [ ] 在独立窗口中按Command+B → 创建一个新的独立窗口
- [ ] 连续快速按Command+B → 不应该创建多个窗口

#### Command+G（图谱窗口）
- [ ] 在主窗口中按Command+G → 只打开一个图谱窗口
- [ ] 切换到独立窗口，按Command+G → 不应该再打开图谱窗口（如果已开）
- [ ] 连续按Command+G → 不应该打开多个图谱窗口

#### Command+M（地图窗口）
- [ ] 在主窗口中按Command+M → 只打开一个地图窗口
- [ ] 在独立窗口中按Command+M → 不应该再打开地图窗口（如果已开）

#### Command+Shift+W（节点管理）
- [ ] 在主窗口中按Command+Shift+W → 只打开一个节点管理窗口
- [ ] 在独立窗口中按Command+Shift+W → 不应该再打开（如果已开）

#### Command+Shift+I（标签管理）
- [ ] 在主窗口中按Command+Shift+I → 只在当前窗口显示标签管理
- [ ] 在独立窗口中按Command+Shift+I → 只在当前窗口显示

#### Command+Shift+F（快速搜索）
- [ ] 在主窗口中按Command+Shift+F → 只在当前窗口显示快速搜索
- [ ] 在独立窗口中按Command+Shift+F → 只在当前窗口显示

#### Command+I（快速添加节点）
- [ ] 在主窗口中按Command+I → 只在当前窗口显示快速添加面板
- [ ] 在独立窗口中按Command+I → 只在当前窗口显示

### 窗口特定命令（只影响当前活跃窗口）

#### Command+E（切换侧边栏）
- [ ] 在主窗口中按Command+E → 只切换当前窗口的侧边栏
- [ ] 在独立窗口中按Command+E → 只切换当前窗口的侧边栏

#### Command+F（标签搜索）
- [ ] 在主窗口中按Command+F → 只在当前窗口的侧边栏切换到搜索模式
- [ ] 在独立窗口中按Command+F → 只在当前窗口响应

#### 详情面板快捷键（需要先选择一个节点）
选择任意节点后测试：

#### Command+O（切换到详情标签）
- [ ] 在主窗口中按Command+O → 只在当前窗口切换到详情标签
- [ ] 在独立窗口中按Command+O → 只在当前窗口切换

#### Command+L（切换到图谱标签）
- [ ] 在主窗口中按Command+L → 只在当前窗口切换到图谱标签
- [ ] 在独立窗口中按Command+L → 只在当前窗口切换

#### Command+T（编辑模式切换）
- [ ] 在主窗口中按Command+T → 只在当前窗口切换到编辑模式
- [ ] 在独立窗口中按Command+T → 只在当前窗口切换

## 多窗口焦点测试

### 窗口切换测试
- [ ] 在主窗口和独立窗口间快速切换，每次切换后按Command+K验证响应的是正确窗口
- [ ] 打开多个独立窗口，在它们之间切换并测试快捷键

### 后台窗口测试
- [ ] 确保非活跃窗口不响应窗口特定命令
- [ ] 确保全局命令只在当前活跃窗口中执行

### 新窗口测试
- [ ] 创建新窗口后立即测试所有快捷键
- [ ] 确保Command+B在新创建的窗口中正常工作

## 边缘情况测试

### 快速操作测试
- [ ] 快速连续按同一个快捷键（测试防抖机制）
- [ ] 在窗口切换过程中按快捷键
- [ ] 同时打开多个相同类型的窗口后测试

### 应用焦点测试
- [ ] 让应用失去焦点，然后重新获得焦点，测试快捷键是否正常
- [ ] 在有其他应用活跃时按WordTagger的快捷键

## 验证成功标准

✅ **所有测试项都应该通过**，表明：

1. **单窗口响应**：每个快捷键只在当前活跃窗口响应
2. **无重复执行**：快速连续按键不会造成重复操作
3. **焦点正确性**：窗口切换后快捷键响应正确的窗口
4. **功能完整性**：所有快捷键功能都正常工作

## 测试完成检查

- [ ] 所有快捷键都只在当前活跃窗口响应
- [ ] Command+B在所有窗口类型中都能正常创建新窗口
- [ ] 没有出现多窗口重复响应的情况
- [ ] 窗口焦点管理正确工作
- [ ] 用户体验流畅统一

如果所有测试项都通过，则快捷键修复成功！ 🎉

## 问题报告

如果发现问题，请记录：
- 问题快捷键：
- 重现步骤：
- 预期行为：
- 实际行为：
- 窗口状态：