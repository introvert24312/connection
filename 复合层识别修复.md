# 复合层识别修复

## 问题描述

当用户在命令面板中输入"层R 层A 层B"这样的格式时，应该被识别为创建复合层的命令（层R 是复合层名，层A 和 层B 是子层），但之前被当作普通层处理了。

## 问题原因

在 `CommandPaletteView.swift` 的 `handleCreateNewLayer()` 函数中，直接创建普通层，没有先检查输入是否符合复合层的格式。

## 修复方案

### 1. 修改 CommandParser.swift ✅

将 `parseDirectCommand` 方法从 `private` 改为 `public`，使其可以被外部调用：

```swift
// 从 private 改为 public
public func parseDirectCommand(_ input: String, context: CommandContext) -> Command?
```

### 2. 修改 CommandPaletteView.swift ✅

在 `handleCreateNewLayer()` 函数中添加复合层格式检查：

```swift
// 处理⌘R: 创建新层
private func handleCreateNewLayer() {
    let trimmedQuery = query.trimmingCharacters(in: .whitespacesAndNewlines)
    guard !trimmedQuery.isEmpty else {
        print("🔍 CommandPalette: 查询为空，无法创建新层")
        return
    }
    
    // 首先检查是否是复合层格式
    let context = CommandContext(store: store)
    if let command = commandParser.parseDirectCommand(trimmedQuery, context: context) {
        // 如果解析出了命令，执行它
        Task {
            do {
                let result = try await command.execute(with: context)
                await MainActor.run {
                    handleCommandResult(result)
                    // 清空搜索框
                    query = ""
                    showSearchDropdown = false
                }
            } catch {
                print("❌ CommandPalette: 执行命令失败: \(error)")
            }
        }
        return
    }
    
    // 如果不是复合层格式，创建普通层
    // ... 原有的普通层创建逻辑
}
```

## 复合层识别逻辑

CommandParser 中的复合层识别支持两种格式：

### 1. 简单格式（推荐）
```
层R 层A 层B
```
- 第一个词是复合层名称
- 后面的词是子层名称

### 2. 传统格式
```
复合层 层R 包含 层A 层B
```
- 使用关键词"复合层"和"包含"

## 工作流程

1. **用户输入**：在命令面板搜索框中输入"层R 层A 层B"
2. **按 Command+R**：触发 `handleCreateNewLayer()` 函数
3. **格式检查**：调用 `commandParser.parseDirectCommand()` 检查是否是复合层格式
4. **命令识别**：如果识别为复合层命令，创建 `CreateCompoundLayerCommand`
5. **执行命令**：异步执行复合层创建逻辑
6. **结果处理**：处理创建结果，清空搜索框

## 测试用例

### ✅ 复合层创建
- 输入：`层R 层A 层B`
- 按 Command+R
- 预期：创建名为"层R"的复合层，包含"层A"和"层B"两个子层

### ✅ 普通层创建
- 输入：`工作层`
- 按 Command+R  
- 预期：创建名为"工作层"的普通层

### ✅ 错误处理
- 输入：`层R 不存在的层`
- 按 Command+R
- 预期：显示错误信息"未找到以下层: 不存在的层"

## 技术细节

- **异步处理**：复合层创建使用 `Task` 进行异步处理
- **错误处理**：包含完整的错误捕获和处理机制
- **状态管理**：成功创建后自动清空搜索框
- **向后兼容**：不影响现有的普通层创建功能

修复完成！现在输入"层R 层A 层B"按 Command+R 应该能正确创建复合层了。