# 多主窗口层切换测试步骤

## 📋 测试步骤

### 1. 准备工作
- 确保WordTagger应用正在运行
- 确保有多个不同的层（比如"我喜欢的歌"、"计算机教程"等）

### 2. 打开多个主窗口
1. **打开第一个主窗口**（应该已经在运行）
2. **打开第二个主窗口**：
   - 使用菜单：`文件 > 新建窗口`
   - 或使用快捷键：`Command+N`
3. **打开第三个主窗口**（可选）：
   - 重复上面的步骤

### 3. 在不同主窗口中设置不同的层
1. **主窗口1**: 切换到层 "我喜欢的歌"
2. **主窗口2**: 切换到层 "计算机教程"  
3. **主窗口3**: 切换到层 "你好"（如果有第三个窗口）

### 4. 测试层图谱切换功能

#### 测试主窗口1的层图谱
1. 在**主窗口1**中按 `Command+K` 打开层图谱
2. 在层图谱中 `Command+点击` 不同的层节点
3. **预期结果**: 只有**主窗口1**的层发生变化，其他主窗口不受影响

#### 测试主窗口2的层图谱
1. 在**主窗口2**中按 `Command+K` 打开层图谱
2. 在层图谱中 `Command+点击` 不同的层节点  
3. **预期结果**: 只有**主窗口2**的层发生变化，其他主窗口不受影响

#### 测试主窗口3的层图谱（如果有）
1. 在**主窗口3**中按 `Command+K` 打开层图谱
2. 在层图谱中 `Command+点击` 不同的层节点
3. **预期结果**: 只有**主窗口3**的层发生变化，其他主窗口不受影响

## 🔍 如何验证修复成功

### 修复前的问题行为：
- 所有层图谱都只能切换第一个主窗口的层
- 第二个、第三个主窗口的层图谱无法影响对应的主窗口

### 修复后的正确行为：
- **主窗口1的层图谱** → 只影响**主窗口1**
- **主窗口2的层图谱** → 只影响**主窗口2** 
- **主窗口3的层图谱** → 只影响**主窗口3**

## 📊 调试日志关键信息

在Console.app或终端中查看以下关键日志：

```
🔗 LayerGraphWindow: 建立窗口映射 - 图谱窗口(XXXXXXXX) <- 启动主窗口(YYYYYYYY)
🔄 LayerGraphWindow: 切换到层 'XXX'
   - 当前层图谱窗口ID: XXXXXXXX
   - 映射的目标主窗口ID: YYYYYYYY
🔄 主窗口: 处理来自层图谱的层切换请求 - 切换到层: XXX
```

**关键验证点:**
- 不同主窗口的层图谱应该有不同的窗口ID
- 每个层图谱应该映射到正确的主窗口ID
- 层切换通知应该只被对应的主窗口处理

## ❌ 如果仍有问题

如果测试发现问题仍然存在，请：
1. 复制完整的控制台日志
2. 说明具体的测试步骤和异常行为
3. 截图显示多个主窗口的状态

## ✅ 预期的成功标准

- ✅ 可以同时打开多个主窗口
- ✅ 每个主窗口可以独立打开层图谱
- ✅ 每个层图谱只影响启动它的主窗口
- ✅ 不同主窗口的层可以独立切换，互不影响