# 🔧 修复图谱消失问题

## 问题分析

从日志可以看出：
1. 图谱成功加载：`✅ [WebView-AC02B6B6] didFinish: 图形加载完成`
2. 但立即失去焦点：`🏠 WindowFocusManager: 窗口失去key`
3. 图谱消失

## 根本原因

当你点击节点时，触发了一系列视图更新：
1. **store.selectedNode 变化** → DetailPanel 重新渲染
2. **NodeGraphView 被重建** → 即使有 `.id()` 修饰符
3. **WebView 加载完成** → 但窗口焦点丢失导致视图被隐藏

## 解决方案

### 1. 防止不必要的视图重建

在 `DetailPanel.swift` 中，优化监听逻辑：

```swift
// 第146行，修改 onChange 监听
.onReceive(store.$selectedNode
    .removeDuplicates { $0?.id == $1?.id }  // 只在节点ID真正变化时触发
    .debounce(for: .milliseconds(200), scheduler: DispatchQueue.main)) { selectedNode in
    // 只有当选中的节点真的发生变化时才更新
    if let selected = selectedNode, selected.id != stableCurrentNode.id {
        updateStableCurrentNode()
    }
}
```

### 2. 保持图谱视图稳定

修改 `NodeGraphView` 的显示逻辑，添加稳定性：

```swift
// 在 DetailPanel 的 body 中
case .related:
    NodeGraphView(node: stableCurrentNode)
        .id("stable-graph-\(stableCurrentNode.id.uuidString)")
        .equatable()  // 添加这个修饰符来优化比较
        .transition(.identity)  // 防止动画导致的视图消失
```

### 3. 修复窗口焦点问题

WindowFocusManager 的日志显示窗口失去焦点，这可能导致某些视图被隐藏。需要确保图谱视图不受窗口焦点影响：

```swift
// 在 NodeGraphView 的 body 中添加
.focusable(false)  // 已经有了
.allowsHitTesting(true)  // 确保可以交互
.frame(maxWidth: .infinity, maxHeight: .infinity)  // 确保占满空间
```

### 4. 添加调试日志

在 `DetailPanel.swift` 的第127-137行附近添加调试日志：

```swift
Group {
    switch tab {
    case .detail:
        NodeDetailView(node: stableCurrentNode)
    case .map:
        NodeMapView(node: stableCurrentNode)
    case .related:
        NodeGraphView(node: stableCurrentNode)
            .id("stable-graph-\(stableCurrentNode.id.uuidString)")
            .onAppear {
                print("🎯 NodeGraphView appeared for node: \(stableCurrentNode.text)")
            }
            .onDisappear {
                print("❌ NodeGraphView disappeared for node: \(stableCurrentNode.text)")
            }
    }
}
```

## 临时解决方案

如果上述方案无效，可以尝试：

1. **禁用动画**：移除 `withAnimation` 包装
2. **强制保持视图**：使用 `ZStack` 而不是条件渲染
3. **延迟更新**：增加防抖时间到 500ms

## 测试步骤

1. 应用修改后重新编译
2. 点击一个节点，观察图谱是否稳定显示
3. 快速切换节点，检查图谱是否闪烁
4. 查看控制台日志，确认视图生命周期