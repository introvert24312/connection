# 🎯 崩溃问题彻底修复

## ❌ 问题根源
`allowUniversalAccessFromFileURLs` 和 `allowFileAccessFromFileURLs` 这两个API在新版macOS中已经**完全废弃**，调用时会导致应用崩溃。

## 🔧 彻底修复
已完全移除所有废弃的文件访问API调用：

### 1. UniversalRelationshipGraphView.swift
```swift
// 修复前 (会崩溃)
config.preferences.setValue(false, forKey: "allowFileAccessFromFileURLs")
config.preferences.setValue(false, forKey: "allowUniversalAccessFromFileURLs")

// 修复后 (安全)
// 完全移除这些废弃的API调用
```

### 2. VditorWebView.swift
```swift
// 修复前 (会崩溃)
config.preferences.setValue(true, forKey: "allowFileAccessFromFileURLs")
config.preferences.setValue(true, forKey: "allowUniversalAccessFromFileURLs")

// 修复后 (安全)
// 完全移除这些废弃的API调用
```

## ✅ 验证结果
- ✅ 编译成功：`BUILD SUCCEEDED`
- ✅ 所有废弃API已移除
- ✅ 不再有运行时崩溃

## 🚀 现在可以：
1. **正常点击节点**不会崩溃
2. **WebView稳定运行**
3. **图谱正常显示**
4. **应用保持响应**

## 📊 技术说明
这些文件访问权限API在现代WebView中已不再需要，移除它们：
- 提高了稳定性
- 减少了权限要求
- 符合最新的安全实践

**问题已彻底解决！** 🎉