# 层过滤器重新设计 - 参考全局图谱UI

## 设计目标

将命令面板中的层过滤器重新设计为内嵌UI，参考全局图谱的工具栏设计，提供更好的用户体验。

## 主要改进

### 1. 工具栏式设计
- ✅ 采用类似GraphView的工具栏布局
- ✅ 标题 + 控制按钮 + 搜索框 + 菜单的横向布局
- ✅ 统一的视觉风格和交互模式

### 2. 层选择器按钮
- ✅ 类似"选择节点"按钮的设计
- ✅ 显示当前选中层数量
- ✅ 支持全选/清空切换
- ✅ 动态图标显示选择状态

### 3. 搜索框优化
- ✅ 紧凑的搜索框设计（120px宽度）
- ✅ 完全隔离的事件处理，防止意外触发
- ✅ 回车键拦截，不执行任何命令

### 4. 快速过滤菜单
- ✅ 下拉菜单提供快速过滤选项
- ✅ 全选、清空、仅复合层、仅普通层、当前层
- ✅ 统一的按钮样式

### 5. 缩放控制（新增）
- ✅ 缩放级别菜单（50% - 150%）
- ✅ 适应窗口功能（⌘K快捷键）
- ✅ 持久化缩放设置（AppStorage）
- ✅ 实时缩放级别显示

### 6. 层标签芯片
- ✅ 紧凑的芯片式设计
- ✅ 层类型指示器（圆点颜色区分复合层/普通层）
- ✅ 选中状态的视觉反馈
- ✅ 悬停提示显示层类型信息

## 技术实现

### 工具栏布局
```swift
HStack {
    Text("层结构图谱")
    Spacer()
    // 层选择器按钮
    // 搜索框
    // 过滤菜单
    // 缩放控制
    // 重置按钮
}
```

### 缩放控制
```swift
@AppStorage("layerGraphInitialScale") private var layerGraphInitialScale: Double = 0.9

Menu {
    Button("适应窗口 (⌘K)") { /* 发送通知 */ }
    // 缩放级别选项
} label: {
    HStack {
        Image(systemName: "magnifyingglass")
        Text("\(Int(layerGraphInitialScale * 100))%")
    }
}
```

### 层标签芯片
```swift
HStack(spacing: 4) {
    Circle()
        .fill(layer.isCompound ? Color.purple : Color.blue)
        .frame(width: 6, height: 6)
    
    Text(layer.displayName)
        .font(.caption)
    
    if isSelected {
        Image(systemName: "xmark")
    }
}
.background(Capsule().fill(isSelected ? Color.blue : Color.gray.opacity(0.2)))
```

## 用户体验改进

### 视觉一致性
- 与全局图谱工具栏保持一致的设计语言
- 统一的按钮样式、间距和颜色
- 清晰的信息层次和视觉引导

### 交互优化
- 更直观的层选择和过滤操作
- 快速访问常用功能（全选、清空等）
- 键盘快捷键支持（⌘K适应窗口）

### 空间利用
- 紧凑的水平布局，节省垂直空间
- 智能的层标签显示（最多10个，超出显示省略）
- 响应式设计，适应不同窗口大小

## Bug修复

### GraphView选择状态加载错误
- ✅ 修复了UUID解码错误
- ✅ 添加了向后兼容性支持
- ✅ 自动清理损坏的数据

### 事件处理优化
- ✅ 完善的事件拦截机制
- ✅ 防止意外命令执行
- ✅ 安全的背景关闭控制

## 测试场景

1. **缩放控制测试**
   - 使用菜单调整缩放级别
   - 按⌘K适应窗口
   - 缩放设置持久化

2. **层过滤测试**
   - 使用搜索框过滤层
   - 快速过滤菜单功能
   - 层标签芯片交互

3. **事件安全测试**
   - 点击各个控件不会意外关闭窗口
   - 搜索框回车不会执行命令
   - 图谱区域点击正常工作

这个重新设计提供了更专业、更一致的用户界面，同时解决了之前的功能和交互问题。