# 窗口切换调试测试指南

## 测试目的
验证层图谱窗口切换功能是否正确识别和切换到正确的窗口。

## 测试步骤

### 1. 准备测试环境
- 启动 Connection 应用
- 打开至少2个主窗口（不同的emoji标题）
- 打开一个层图谱窗口

### 2. 测试窗口切换检测
1. 点击窗口A（例如标题为"🏠"的窗口）
2. 观察Console中的调试输出，应该看到：
   ```
   🔄 LayerGraphWindow: 检测到窗口切换 - '🏠'
   🔍 === 详细调试：窗口切换分析 ===
   📋 用户看到的窗口标题: '🏠'
   📊 系统中所有注册窗口:
   ⭐️[当前记录的目标] 应该标记窗口A
   ```

### 3. 测试Command+点击层切换
1. 在层图谱中 Command+点击任意层节点
2. 观察Console中的调试输出，应该看到：
   ```
   🚀 === 层图谱Command+点击调试 ===
   🎯 用户Command+点击了层: 'xxx'
   📊 系统中所有注册窗口:
   ⭐️[目标窗口] 应该是之前点击的窗口A
   📡 === 发送层切换通知 ===
   ⚠️  注意观察：这个窗口应该会收到层切换通知并切换到指定层
   ```

### 4. 验证实际行为
- **期望行为**: Command+点击后，窗口A应该被激活并切换到对应层
- **错误行为**: 如果窗口C被激活而不是窗口A，说明存在窗口映射错误

## 问题分析

如果看到调试日志显示正确，但实际行为错误，可能的原因：

1. **NSWindow映射错误**: `windowToUUIDMap` 映射了错误的窗口实例
2. **通知接收错误**: 正确的窗口ID收到了通知，但对应的NSWindow实例不正确
3. **时序问题**: 窗口切换检测和实际窗口激活之间的时间差

## 调试命令

在Terminal中运行以下命令查看实时日志：
```bash
log stream --predicate 'subsystem == "com.apple.console" AND category == "default"' | grep -E "(WindowFocusManager|LayerGraphWindow)"
```

## 预期的修复效果

修复后应该看到：
- 调试日志中的目标窗口ID与实际激活的窗口一致
- Command+点击层图谱后，正确的窗口被激活
- 不再出现"点击窗口A但切换到窗口C"的问题