# 层管理修复总结

## 问题描述

你遇到了两个主要问题：

1. **Command+R 无法添加新层**：在命令面板的搜索框中输入层名后，按 Command+R 无法创建新层
2. **设置中创建新层的UI布局问题**：设置界面中的"创建新层"弹窗UI挤在左边，布局混乱

## 修复内容

### 1. Command+R 快捷键支持 ✅

**文件：** `WordTagger/CommandPaletteView.swift`

**修复内容：**
- 在搜索框的 `onKeyPress` 处理中添加了对 `r` 和 `R` 键的监听
- 添加了 `handleCreateNewLayer()` 函数来处理新层创建逻辑
- 在 NSEvent 监听器中添加了对 Command+R (keyCode=15) 的支持

**新增功能：**
```swift
// 处理⌘R: 创建新层
private func handleCreateNewLayer() {
    let trimmedQuery = query.trimmingCharacters(in: .whitespacesAndNewlines)
    guard !trimmedQuery.isEmpty else {
        print("🔍 CommandPalette: 查询为空，无法创建新层")
        return
    }
    
    // 检查是否已存在同名层
    let existingLayer = store.layers.first { layer in
        layer.displayName.lowercased() == trimmedQuery.lowercased() ||
        layer.name.lowercased() == trimmedQuery.lowercased()
    }
    
    if existingLayer != nil {
        print("⚠️ CommandPalette: 层 '\(trimmedQuery)' 已存在")
        return
    }
    
    // 创建新层
    let newLayer = store.createLayer(
        name: trimmedQuery.lowercased().replacingOccurrences(of: " ", with: "_"),
        displayName: trimmedQuery,
        color: "blue"
    )
    
    print("✅ CommandPalette: 成功创建新层 '\(newLayer.displayName)'")
    
    // 自动添加新层到过滤器
    filteredLayerIds.insert(newLayer.id)
    
    // 清空搜索框
    query = ""
    showSearchDropdown = false
    
    // 如果这是第一个层，自动激活
    if store.layers.count == 1 {
        Task {
            await store.switchToLayer(newLayer)
        }
    }
}
```

### 2. 创建新层UI布局修复 ✅

**文件：** `WordTagger/SettingsView.swift`

**修复内容：**
- 移除了有问题的 `NavigationView`，改用 `VStack` 布局
- 添加了自定义的顶部标题栏，包含标题和操作按钮
- 重新组织了内容区域的布局结构
- 调整了窗口尺寸从 480x500 到 500x550
- 确保内容区域使用 `maxWidth: .infinity, maxHeight: .infinity` 来正确填充空间

**新的布局结构：**
```swift
VStack(spacing: 0) {
    // 顶部标题栏
    HStack {
        Text("创建新层")
            .font(.title2)
            .fontWeight(.bold)
        
        Spacer()
        
        HStack(spacing: 12) {
            Button("取消") { dismiss() }
                .buttonStyle(.bordered)
            
            Button("创建") { /* 创建逻辑 */ }
                .buttonStyle(.borderedProminent)
                .disabled(!isFormValid)
        }
    }
    .padding(.horizontal, 24)
    .padding(.vertical, 16)
    .background(Color(NSColor.controlBackgroundColor))
    
    Divider()
    
    // 主内容区域
    ScrollView {
        // 表单内容
    }
    .frame(maxWidth: .infinity, maxHeight: .infinity)
}
.frame(width: 500, height: 550)
.background(Color(NSColor.windowBackgroundColor))
```

## 使用方法

### Command+R 创建新层
1. 按 Command+K 打开命令面板
2. 在搜索框中输入新层的名称（例如："工作层"、"学习层"等）
3. 按下 Command+R
4. 新层将被创建并自动添加到图谱显示中
5. 搜索框会自动清空

### 设置中创建新层
1. 打开设置（Command+,）
2. 切换到"层管理"标签
3. 点击"创建新层"按钮
4. 在弹出的窗口中填写层信息
5. UI现在应该正确显示，不会挤在左边

## 测试验证

构建已成功完成，你现在可以：

1. **测试 Command+R 功能**：
   - 打开命令面板
   - 输入一个新层名称
   - 按 Command+R 验证是否创建成功

2. **测试设置UI**：
   - 打开设置 → 层管理
   - 点击"创建新层"
   - 验证UI布局是否正常

## 技术细节

- **键盘事件处理**：使用了 SwiftUI 的 `onKeyPress` 和 NSEvent 监听器双重保障
- **UI布局**：移除了导致布局问题的 NavigationView，使用更可控的 VStack 结构
- **状态管理**：确保新创建的层自动添加到过滤器中，提供更好的用户体验
- **错误处理**：添加了重名检查，避免创建重复的层

修复完成！你的层管理功能现在应该完全正常工作了。