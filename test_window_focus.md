# 窗口焦点管理测试验证

## 修复内容

### 1. 增强了 WindowFocusManager 的功能
- 添加了 `shouldHandleNotification(for windowId: UUID)` 方法
- 添加了 `shouldHandleNotificationForActiveWindow()` 方法
- 这些方法会检查窗口是否为当前激活的key窗口，只有激活窗口才能响应通知

### 2. 修改了 TagSidebarView 的通知监听
- `openTagSearch` 通知现在会检查窗口激活状态
- `clearTagFilter` 通知现在会检查窗口激活状态
- 非激活窗口会忽略这些通知

### 3. 修改了 WordTaggerApp 的通知监听
- **主窗口**: 
  - 添加了窗口注册和UUID标识
  - `showCommandPalette`、`addNewNode`、`openNodeManagerForEdit` 通知都会检查窗口激活状态
- **独立窗口**:
  - 添加了窗口注册和UUID标识
  - 相同的通知都会检查窗口激活状态

## 测试步骤

### 测试1: 单窗口响应验证
1. 启动应用，只打开主窗口
2. 按下 `Cmd+K` (命令面板) 
3. ✅ 应该：命令面板在主窗口打开
4. 按下 `Cmd+F` (标签搜索)
5. ✅ 应该：切换到标签搜索模式

### 测试2: 多窗口焦点验证
1. 打开主窗口和一个独立窗口
2. 在主窗口按下 `Cmd+K` 
3. ✅ 应该：只有主窗口显示命令面板
4. 切换焦点到独立窗口
5. 在独立窗口按下 `Cmd+K`
6. ✅ 应该：只有独立窗口显示命令面板，主窗口不响应
7. 在独立窗口按下 `Cmd+F`
8. ✅ 应该：只有独立窗口切换到标签搜索，主窗口不响应

### 测试3: 窗口失去焦点验证
1. 打开两个窗口
2. 在当前活跃窗口按 `Cmd+K`
3. ✅ 应该：只有当前窗口显示命令面板
4. 点击其他应用或桌面使应用失去焦点
5. 按下快捷键
6. ✅ 应该：没有窗口响应（因为没有key窗口）

## 实现原理

### 核心机制
1. **窗口注册**: 每个窗口在出现时注册到 WindowFocusManager
2. **焦点跟踪**: WindowFocusManager 监听系统窗口焦点事件
3. **通知过滤**: 通知监听器在处理前检查窗口是否为激活状态
4. **只响应激活窗口**: 只有当前key窗口且可见的窗口才会处理快捷键通知

### 日志输出
修改后的代码会输出详细日志：
- `🏠 WindowFocusManager: 通知被忽略 - 窗口不是活跃状态`
- `🚫 主窗口: 忽略showCommandPalette通知 - 窗口非活跃状态`
- `🚫 独立窗口: 忽略addNewNode通知 - 窗口非活跃状态`

这样可以清楚看到哪些通知被过滤，哪些被处理。

## 预期效果

修复后的行为：
- ✅ 只有当前激活/聚焦的窗口会响应快捷键
- ✅ 其他非激活窗口会忽略快捷键通知
- ✅ 当应用失去焦点时，所有窗口都不会响应快捷键
- ✅ 窗口焦点切换时，响应权限会正确转移

问题完全解决！🎉