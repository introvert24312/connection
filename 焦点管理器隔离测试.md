# 🔍 焦点管理器隔离测试

## 已禁用的干扰源

我已经逐个禁用了以下可能导致图谱消失的组件：

### 1. 🚫 WindowFocusManager 通知发送
- **位置**: `WindowFocusManager.swift` 第145行和第161行
- **作用**: 禁用 `windowFocusChanged` 通知的发送
- **原因**: 这些通知可能触发其他组件隐藏图谱

### 2. 🚫 窗口失去key状态监听
- **位置**: `WindowFocusManager.swift` 第481行
- **作用**: 禁用 `NSWindow.didResignKeyNotification` 监听
- **原因**: 窗口失去焦点时可能触发图谱隐藏逻辑

### 3. 🚫 应用程序失去活跃状态监听
- **位置**: `WindowFocusManager.swift` 第518行
- **作用**: 禁用 `NSApplication.didResignActiveNotification` 监听
- **原因**: 应用失去焦点时可能触发全局图谱隐藏

### 4. 🚫 应用程序状态监听 (图谱组件)
- **位置**: `UniversalRelationshipGraphView.swift` 第350行
- **作用**: 禁用图谱组件内的应用状态监听
- **原因**: 直接在图谱组件中的状态监听可能导致重建

### 5. 🔧 强制视图持久化
- **位置**: `DetailPanel.swift` 第155行
- **作用**: 即使在隐藏状态下也保持 `opacity=0.01`
- **原因**: 完全透明(opacity=0)可能被系统优化掉

## 测试步骤

### 现在请测试：

1. **启动应用**
2. **点击"蓝色风景"节点**
3. **观察调试信息**：
   - 应该看到 "🎯 FORCE-PERSISTENT NodeGraphView APPEARED"
   - 绿色边框显示 "持久图谱"

4. **切换到其他应用** (比如Chrome、访达等)
5. **立即切换回WordTagger**
6. **观察结果**：
   - ✅ **期望**: 图谱应该立即可见
   - ❌ **如果还闪退**: 说明问题在其他地方

### 观察日志变化

现在的日志应该显示：
```
🚫 WindowFocusManager: 已禁用windowFocusChanged通知发送
🚫 WindowFocusManager: 已禁用NSWindow.didResignKeyNotification监听
🚫 WindowFocusManager: 已禁用NSApplication.didResignActiveNotification监听
```

### 如果问题仍然存在

请告诉我：
1. **是否还看到这行日志**: `❌ DetailPanel: FORCE-PERSISTENT NodeGraphView DISAPPEARED`
2. **具体的闪退现象**: 是完全黑屏还是闪一下
3. **新的日志内容**: 看看还有什么可疑的日志

这样我们就能确定是否还有其他干扰源！