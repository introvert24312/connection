# 层结构图谱缩放设置改进

## 改进内容

### ✅ 移除了命令面板中的缩放控制菜单
- 不再在命令面板工具栏中显示缩放级别下拉菜单
- 保持界面简洁，避免功能冗余

### ✅ 使用设置中的统一缩放控制
- 层结构图谱的缩放级别现在通过设置界面统一管理
- 设置路径：**设置 > 常规 > 界面设置 > 层结构图谱初始缩放**
- 支持0.5x到3.0x的缩放范围，步长0.1x

### ✅ 保留了⌘K适应窗口功能
- 用户仍可以使用⌘K快捷键让图谱适应窗口大小
- 这是一个实用的快捷功能，不需要通过设置调整

## 技术实现

### AppStorage键名统一
```swift
// 之前
@AppStorage("layerGraphInitialScale") private var layerGraphInitialScale: Double = 0.9

// 现在
@AppStorage("layerStructureGraphInitialScale") private var layerGraphInitialScale: Double = 0.9
```

### 设置界面中的控制
```swift
SettingRow(
    title: "层结构图谱初始缩放",
    description: "命令面板中层结构图谱的默认缩放级别"
) {
    HStack(spacing: 8) {
        Text("\(String(format: "%.1f", layerStructureGraphInitialScale))x")
            .foregroundColor(.secondary)
            .frame(width: 40, alignment: .trailing)
        
        Slider(value: $layerStructureGraphInitialScale, in: 0.5...3.0, step: 0.1)
            .frame(width: 120)
    }
}
```

## 用户体验改进

### 统一的设置管理
- 所有图谱的缩放设置都在一个地方管理
- 用户不需要在多个地方寻找相同的功能
- 设置界面提供了清晰的描述和实时预览

### 简洁的命令面板界面
- 移除了不必要的UI元素
- 工具栏更加简洁，专注于核心功能
- 减少了界面复杂度

### 灵活的缩放控制
- 0.5x - 3.0x的宽泛缩放范围
- 0.1x的精细步长控制
- 实时显示当前缩放值

## 使用方法

### 调整默认缩放级别
1. 打开设置（⌘,）
2. 选择"常规"标签
3. 在"界面设置"组中找到"层结构图谱初始缩放"
4. 使用滑块调整到合适的缩放级别
5. 设置会自动保存并应用到新打开的命令面板

### 临时调整图谱大小
- 在命令面板中按⌘K可以让图谱适应当前窗口大小
- 这不会改变默认的缩放设置

## 设置项说明

| 设置项 | 描述 | 范围 | 默认值 |
|--------|------|------|--------|
| 全局图谱初始缩放 | 全局图谱窗口的默认缩放 | 0.5x - 3.0x | 1.0x |
| 详情图谱初始缩放 | 节点详情图谱的默认缩放 | 0.5x - 3.0x | 1.0x |
| 全屏图谱初始缩放 | Command+D全屏图谱的默认缩放 | 0.5x - 3.0x | 1.0x |
| **层结构图谱初始缩放** | **命令面板中层结构图谱的默认缩放** | **0.5x - 3.0x** | **0.9x** |

这个改进让缩放设置更加统一和专业，同时保持了界面的简洁性。用户现在可以在一个地方管理所有图谱的缩放设置，而不需要在每个界面中单独调整。